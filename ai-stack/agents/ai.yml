name: AI Pipeline

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4

      - run: pip install pytest flake8 bandit

      - run: flake8 .
      - run: bandit -r .
      - run: pytest